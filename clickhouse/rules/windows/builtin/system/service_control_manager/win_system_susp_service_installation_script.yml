id: 70f00d10-60b2-4f34-b9a0-dc3df3fe762a
name: Suspicious Service Installation Script
level: 8
size: '>0'
active: true
author:
- pH-T (Nextron Systems)
group: windows
tags:
- attack.persistence
- attack.privilege-escalation
- car.2013-09-005
- attack.t1543.003
rule: SELECT * FROM logs WHERE (Provider_Name='Service Control Manager' AND EventID=7045) AND (ImagePath ILIKE '% -c %' OR ImagePath ILIKE '% /c %' OR ImagePath ILIKE '% –c %' OR ImagePath ILIKE '% —c %' OR ImagePath ILIKE '% ―c %' OR ImagePath ILIKE '% -r %' OR ImagePath ILIKE '% /r %' OR ImagePath ILIKE '% –r %' OR ImagePath ILIKE '% —r %' OR ImagePath ILIKE '% ―r %' OR ImagePath ILIKE '% -k %' OR ImagePath ILIKE '% /k %' OR ImagePath ILIKE '% –k %' OR ImagePath ILIKE '% —k %' OR ImagePath ILIKE '% ―k %') AND (ImagePath ILIKE '%cscript%' OR ImagePath ILIKE '%mshta%' OR ImagePath ILIKE '%powershell%' OR ImagePath ILIKE '%pwsh%' OR ImagePath ILIKE '%regsvr32%' OR ImagePath ILIKE '%rundll32%' OR ImagePath ILIKE '%wscript%')
